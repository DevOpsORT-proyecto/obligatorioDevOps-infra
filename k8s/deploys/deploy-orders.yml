apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-deployment
  labels:
    app: orders
spec:
  replicas: 2
  template:
    metadata:
      name: orders-pod
      labels:
        app: orders
    spec:
      containers:
        - name: orders
          image: southamerica-east1-docker.pkg.dev/proyectodevopsort/proyecto-devops-ort/orders-microservice:latest
          env:
            - name: paymentsUrl
              value: http://payments-service/payments
            - name: shippingUrl
              value: http://shipping-service/shipping
            - name: productsUrl
              value: http://products-service/products

          ports:
            - containerPort: 8080
  selector:
    matchLabels:
      app: orders